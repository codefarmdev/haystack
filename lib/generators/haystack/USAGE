Description:
    Haystack install generator

Example:
    rails generate haystack PUSH_KEY

    This will create config/haystack.yml with your PUSH_KEY.
    If you use capistrano it will load Haystack in the deploy file.
